# Docker Compose Override Example
# Copy this to docker-compose.override.yml and customize as needed
# This file will be automatically merged with docker-compose.yml

version: "3.8"

services:
  webserver:
    # Uncomment to use a specific version instead of latest
    # image: ghcr.io/paperless-ngx/paperless-ngx:2.5.3
    
    # Uncomment to set resource limits
    # deploy:
    #   resources:
    #     limits:
    #       cpus: '2.0'
    #       memory: 2G
    #     reservations:
    #       cpus: '1.0'
    #       memory: 1G
    
    # Uncomment to add additional environment variables
    # environment:
    #   PAPERLESS_CONSUMER_POLLING: 60
    #   PAPERLESS_CONSUMER_DELETE_DUPLICATES: "true"
    #   PAPERLESS_CONSUMER_RECURSIVE: "true"
    #   PAPERLESS_CONSUMER_SUBDIRS_AS_TAGS: "true"
    #   PAPERLESS_OCR_SKIP_ARCHIVE_FILE: "never"
    #   PAPERLESS_TASK_WORKERS: 2
    #   PAPERLESS_THREADS_PER_WORKER: 1
    
    # Uncomment to add additional volumes
    # volumes:
    #   - /path/to/additional/consume:/usr/src/paperless/consume2

  # Uncomment to add Gotenberg (for Office document support)
  # gotenberg:
  #   image: docker.io/gotenberg/gotenberg:7
  #   restart: unless-stopped
  #   networks:
  #     - paperless
  #   command:
  #     - "gotenberg"
  #     - "--chromium-disable-javascript=true"
  #     - "--chromium-allow-list=file:///tmp/.*"

  # Uncomment to add Tika (for additional document format support)
  # tika:
  #   image: docker.io/apache/tika:latest
  #   restart: unless-stopped
  #   networks:
  #     - paperless
